# `WebBrowser.get_title()` 初次测试

## 用途

读取当前网页的实时标题；页面导航后再次读取应返回新页面标题。

## 真实验收结果

**VERIFIED：7/7 通过，退出码 0。**

| 测试项 | 结果 |
|---|---|
| API 合同 | 通过，无公开参数，返回标题字符串 |
| 页面准备 | 通过，成功创建 `WebBrowser` |
| 目标标题 | 通过，返回非空标题 |
| 返回类型 | 通过，类型为 `str` |
| 重复读取 | 通过，结果稳定 |
| 导航反映 | 通过，导航到百度后标题正确更新 |
| 资源清理 | 通过，测试页面已关闭 |

测试脚本：`web/test_web_browser_get_title.py`
